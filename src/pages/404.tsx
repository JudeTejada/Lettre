import type { NextPage } from 'next';

const Home: NextPage = () => {
  return (
    <div className='relative min-h-screen bg-primary-bg'>
      <h1>Ooops! Page can't be found</h1>
    </div>
  );
};

export default Home;
